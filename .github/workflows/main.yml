name: انتقال فایل به گیت هاب

on:
  workflow_dispatch:
    inputs:
      file_url:
        description: 'لینک مستقیم فایل مورد نظر'
        required: true
        type: string
      zip_password:
        description: 'رمز عبور برای فایل فشرده'
        required: true
        type: string
      delete_old_archives:
        description: 'پاک کردن فایل‌های قبلی (در صورت تیک زدن، اگه فایل‌های قدیمی در مخزن باشن اتوماتیک حذف میشن)'
        required: false
        type: boolean
        default: false  

permissions:
  contents: write

jobs:
  leech-split-file:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Clean old archives (if requested)
        if: ${{ github.event.inputs.delete_old_archives == 'true' }}
        run: |
         echo "حذف فایل‌های قدیمی آرشیو درخواست شد."
    
         find . -maxdepth 1 -type f -name "*.7z.*" -exec rm -v {} \;
         find . -maxdepth 1 -type f -name "*.7z" -exec rm -v {} \;
         echo "فایل‌های قدیمی آرشیو پاک شدند."
    
         git rm --cached *.7z.* 2>/dev/null || echo "No .7z.* files to unstage."
         git rm --cached *.7z 2>/dev/null || echo "No .7z files to unstage."

      - name: Install 7z
        run: |
          sudo apt-get update
          sudo apt-get install -y p7zip-full

      - name: Download file
        id: download_file
        run: |
          curl -L --fail --retry 3 -o downloaded_file "${{ github.event.inputs.file_url }}"
          echo "File downloaded. Size: $(ls -lh downloaded_file | awk '{print $5}')"
          echo "downloaded_file_name=downloaded_file" >> $GITHUB_OUTPUT

      - name: Extract original filename and prepare for archive
        id: get_filename
        run: |
          DOWNLOADED_FILE="${{ steps.download_file.outputs.downloaded_file_name }}"
          ORIGINAL_URL="${{ github.event.inputs.file_url }}"

          
          FILENAME_FROM_URL=$(basename "$ORIGINAL_URL")
          if [[ -z "$FILENAME_FROM_URL" || "$FILENAME_FROM_URL" == "/" ]]; then
            FILENAME_FROM_URL="$DOWNLOADED_FILE"
          fi
          
          
          FILENAME_BASE=$(echo "$FILENAME_FROM_URL" | sed 's/\.[^.]*$//') 
          FILENAME_EXT="${FILENAME_FROM_URL##*.}" 

          
          if [[ "$FILENAME_BASE" == "$FILENAME_FROM_URL" ]]; then
            FILENAME_EXT=""
          fi

          
          ARCHIVE_PREFIX="sci-hub.ir_"

          
          FULL_ARCHIVE_NAME="${ARCHIVE_PREFIX}${FILENAME_BASE}"

          
          MAX_TOTAL_LEN=50
          CURRENT_LEN=${#FULL_ARCHIVE_NAME}

          if [[ $CURRENT_LEN -gt $MAX_TOTAL_LEN ]]; then
            BASE_LEN_TO_KEEP=$((MAX_TOTAL_LEN - ${#ARCHIVE_PREFIX} - ${#FILENAME_EXT} - 1))
            if [[ $BASE_LEN_TO_KEEP -lt 1 ]]; then BASE_LEN_TO_KEEP=1; fi
            SHORT_FILENAME_BASE=$(echo "$FILENAME_BASE" | cut -c 1-$BASE_LEN_TO_KEEP)
            FULL_ARCHIVE_NAME="${ARCHIVE_PREFIX}${SHORT_FILENAME_BASE}"
          fi
          
          
          if [[ -n "$FILENAME_EXT" ]]; then
            FINAL_ARCHIVE_FILENAME="${FULL_ARCHIVE_NAME}.${FILENAME_EXT}"
          else
            FINAL_ARCHIVE_FILENAME="${FULL_ARCHIVE_NAME}"
          fi

          echo "Original file name: ${FILENAME_FROM_URL}"
          echo "Archive base name will be: ${FINAL_ARCHIVE_FILENAME}"

          
          echo "archive_base_name=${FULL_ARCHIVE_NAME}" >> $GITHUB_OUTPUT
          echo "downloaded_file_name=${DOWNLOADED_FILE}" >> $GITHUB_OUTPUT
          echo "file_extension=${FILENAME_EXT}" >> $GITHUB_OUTPUT
          echo "final_archive_name=${FINAL_ARCHIVE_FILENAME}" >> $GITHUB_OUTPUT

      - name: Rename downloaded file
        id: rename_file
        run: |
          ARCHIVE_NAME_PREFIX="${{ steps.get_filename.outputs.archive_base_name }}"
          DOWNLOADED_FILE="${{ steps.get_filename.outputs.downloaded_file_name }}"
          EXT="${{ steps.get_filename.outputs.file_extension }}"

          NEW_ORIGINAL_FILENAME="$ARCHIVE_NAME_PREFIX"
          if [[ -n "$EXT" ]]; then
            NEW_ORIGINAL_FILENAME="${ARCHIVE_NAME_PREFIX}.${EXT}"
          fi

          mv "$DOWNLOADED_FILE" "$NEW_ORIGINAL_FILENAME"
          echo "Renamed downloaded file to: $NEW_ORIGINAL_FILENAME"
          echo "new_original_filename=$NEW_ORIGINAL_FILENAME" >> $GITHUB_OUTPUT

      - name: Generate timestamp for unique archive
        id: timestamp
        run: echo "value=$(date +%Y%m%d_%H%M%S)" >> $GITHUB_OUTPUT

      - name: Create encrypted split archive
        run: |
          ARCHIVE_NAME_PREFIX="${{ steps.get_filename.outputs.archive_base_name }}_${{ steps.timestamp.outputs.value }}"
          NEW_ORIGINAL_FILENAME="${{ steps.rename_file.outputs.new_original_filename }}"

          #  7zip split archive into 85MB parts
          7z a -p"${{ github.event.inputs.zip_password }}" -mhe=on -mx=3 -v85m "${ARCHIVE_NAME_PREFIX}.7z" "$NEW_ORIGINAL_FILENAME"

          if [ -f "${ARCHIVE_NAME_PREFIX}.7z.001" ] && [ ! -f "${ARCHIVE_NAME_PREFIX}.7z.002" ]; then
          mv "${ARCHIVE_NAME_PREFIX}.7z.001" "${ARCHIVE_NAME_PREFIX}.7z"
          echo "Single chunk - renamed to .7z"
          fi
          
      - name: Remove original downloaded file
        run: |
          FILE="${{ steps.rename_file.outputs.new_original_filename }}"
          if [[ -n "$FILE" && -f "$FILE" ]]; then
            rm "$FILE"
          fi

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"

      - name: Commit and Push chunks
        run: |
          FINAL_ARCHIVE_PREFIX="${{ steps.get_filename.outputs.archive_base_name }}_${{ steps.timestamp.outputs.value }}"
          echo "Adding archive files matching pattern: ${FINAL_ARCHIVE_PREFIX}.7z.*"

          git add "${FINAL_ARCHIVE_PREFIX}.7z."* || echo "No split 7z chunks found."
          git add "${FINAL_ARCHIVE_PREFIX}.7z" || true

          echo "Added archive chunks to staging."

          if ! git diff --staged --quiet; then
            git commit -m "Add archive chunks for ${{ steps.get_filename.outputs.final_archive_name }}"
            echo "Committed all archive chunks."

            push_success=false
            max_attempts=15

            for attempt in $(seq 1 $max_attempts); do
              echo "Attempt $attempt of $max_attempts to push all changes..."
              if git push origin HEAD; then
                echo "Push successful."
                push_success=true
                break
              else
                echo "Push failed on attempt $attempt. Retrying in 20 seconds..."
                sleep 20
                git fetch origin
              fi
            done

            if [ "$push_success" = false ]; then
              echo "Failed to push all chunks after multiple attempts. Attempting force push with lease..."
              if git push origin HEAD --force-with-lease; then
                echo "Force push successful."
              else
                echo "Force push also failed. Aborting."
                exit 1
              fi
            fi
          else
            echo "No changes to commit or push."
          fi
